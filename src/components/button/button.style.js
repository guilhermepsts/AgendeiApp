import {COLORS, FONT_SIZE} from "../../constants/theme.js"

export const styles = {
    btn: {
        width: "100%",
        backgroundColor: COLORS.blue,
        borderRadius: 4,
        padding: 12,
    },

    text: {
        color: COLORS.white,
        fontSize: FONT_SIZE.md,
        textAlign: "center"

    }
}